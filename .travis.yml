language: ruby
sudo: required
notifications:
  slack:
    on_failure: always

matrix:
  fast_finish: true
  include:

    # - name: "provision Vagrantfile template w puppet - bionic"
    #   dist: bionic
    #   language: python
    #   python: 3.7
    #   before_install:
    #     - pip3 install virtualenv
    #     - virtualenv -p $(which python3) ~venvpy3
    #     - source ~venvpy3/bin/activate
    #     - pip install -r requirements.txt
    #   script:
    #       - sudo make deploy-libvirt
    #       - sudo make deploy-vagrant                             
    #       - sudo make provision-puppet
    #       - curl http://192.168.50.10:8080/
    #   after_success:
    #     - deactivate      

    # - name: "build Vagrantfile template with Libvirt KVM QEMU - bionic"
    #   dist: bionic
    #   language: python
    #   python: 3.7
    #   before_install:
    #     - pip3 install virtualenv
    #     - virtualenv -p $(which python3) ~venvpy3
    #     - source ~venvpy3/bin/activate
    #     - pip install -r requirements.txt
    #   script:
    #       - sudo make deploy-libvirt
    #       - sudo make deploy-vagrant
    #       - sudo make deploy-boxes
    #       # - travis_wait 20 waitfortrav.sh
    #   after_success:
    #     - deactivate     

    # TESTED OK
    - name: "typing Vagrantfile - bionic"
      dist: bionic
      language: python
      python: 3.7
      before_install:
        - pip3 install virtualenv
        - virtualenv -p $(which python3) ~venvpy3
        - source ~venvpy3/bin/activate
        - pip install -r requirements.txt
      script:
          - sudo make deploy-libvirt
          - sudo make deploy-vagrant
          - sudo make deploy-vagrantfile          
          # - travis_wait 20 waitfortrav.sh
      after_success:
        - deactivate     

    # TESTED OK
    - name: "build Vagrantfile template - bionic"
      dist: bionic
      language: python
      python: 3.7
      before_install:
        - pip3 install virtualenv
        - virtualenv -p $(which python3) ~venvpy3
        - source ~venvpy3/bin/activate
        - pip install -r requirements.txt
      script:
          - sudo make deploy-libvirt
          - sudo make deploy-vagrant                   
          - sudo vagrant init --template scripts/Vagrantfile.min.erb --var box_name="centos/8" 
          - sudo vagrant up
      after_success:
        - deactivate      

    # TESTED OK        
    - name: "boot Vagrantfile template with Libvirt KVM QEMU - bionic"
      dist: bionic
      language: python
      python: 3.7
      before_install:
        - pip3 install virtualenv
        - virtualenv -p $(which python3) ~venvpy3
        - source ~venvpy3/bin/activate
        - pip install -r requirements.txt
      script:
          - sudo make deploy-libvirt
          - sudo make deploy-vagrant
          - sudo vagrant up
          # - travis_wait 20 waitfortrav.sh
      after_success:
        - deactivate          